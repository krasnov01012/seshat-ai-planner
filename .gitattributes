# Разработка идёт на Windows, эксплуатация — на Linux. Без этого файла Git
# подставит CRLF в рабочую копию, и всё, что исполняется в контейнере,
# сломается на первом же переводе строки.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
