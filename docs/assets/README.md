# Documentation assets

`seshat-architecture.svg` is an original vector diagram prepared for the
Krasnoff employer-facing portfolio and copied into this repository so the README
remains self-contained. Its content is derived from `docs/ARCHITECTURE.md` and
the verified README status table.

The diagram contains no screenshots, third-party artwork, private deployment
coordinates, credentials, or runtime data.
